import React from "react";

import TextPanel from "./TextPanel";

const App: React.FC = () => {
  return (
    <div>
      <TextPanel>{"Let's try this again..."}</TextPanel>
    </div>
  );
};

export default App;
